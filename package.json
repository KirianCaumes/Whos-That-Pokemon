{
  "name": "whosthatpokemon",
  "version": "1.0.0",
  "description": "",
  "main": "back/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd back && npm start",
    "heroku-postbuild": "cd back && npm install && cd ../front && npm install && npm run build && mv build ../back/client"
  },
  "author": "KirianCaumes",
  "engines": {
    "node": "14.15.x"
  }
}
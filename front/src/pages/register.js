
import React from "react"
import { AppProps } from "app"// eslint-disable-line

/**
 * @typedef {object} RegsiterProps
 * @property {any} example
 */

/**
 * @param {RegsiterProps & AppProps} props
 */
export default function Regsiter({ example }) {
    return (
        <>
            <main className="app-register">
                Regsiter
            </main>
        </>
    )
}
